# Discord Bot AI

Многофункциональный Discord бот на базе **LlamaIndex** и **OpenAI**, объединяющий возможности умного ассистента, систему рангов и развлечения.

## 🚀 Основные возможности

### 🧠 Искусственный интеллект (RAG)
- **Контекстный диалог**: Бот помнит контекст переписки и анализирует историю сервера для более точных ответов.
- **Интеграция с инструментами (MCP)**: Может искать информацию в интернете и проверять погоду.
- **Гибкая настройка**: Поддержка переключения между AI-провайдерами.

### 🏆 Система активности
- **Ранги пользователей**: Отслеживание активности и присвоение уникальных рангов.
- **Поздравления**: Автоматические креативные поздравления с днём рождения, сгенерированные AI.

### 📺 Интеграции
- **YouTube Notifier**: Отслеживание новых видео на каналах и оповещение в Discord.
- **Telegram бот**: Дублирование важных уведомлений и ошибок в Telegram.

## 🛠 Команды

| Команда | Описание |
| :--- | :--- |
| `!help` | Показать список доступных команд |
| `!rank [list]` | Узнать свой ранг или список всех рангов |
| `!birthday <дата>` | Сохранить дату рождения (формат: ДД.ММ) |
| `!ai [name]` | Переключить AI-провайдера (Admin) |
| `!youtube <on/off> <name>` | Включить/выключить уведомления канала |
| `!add_youtube <id> <ch_id> <name>` | Добавить YouTube канал (Admin) |
| `!update_user` | Обновить базу пользователей сервера (Admin) |
| `!reset` | Очистить историю сообщений сервера (Admin) |

## ⚙️ Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repo_url>
   cd Discord_BotAI
   ```

2. **Установите зависимости (используется `uv`):**
   ```bash
   uv sync
   # Или через pip:
   pip install -r requirements.txt
   ```

3. **Настройте `.env`:**
   Создайте файл `.env` и добавьте необходимые переменные (токен Discord, OpenAI API key и др.).

4. **Запустите бота:**
   ```bash
   python main.py
   ```

## 📚 Стек технологий

- **Python 3.13+**
- **discord.py** (взаимодействие с API Discord)
- **LlamaIndex** (RAG, работа с LLM)
- **ChromaDB** (векторная база данных)
- **SQLAlchemy & asyncpg** (PostgreSQL)
- **MCP (Model Context Protocol)** (инструменты для AI)
